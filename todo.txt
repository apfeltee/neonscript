
specification of custom keywords:

    keyword(echo) = function(args, ...)
    {
    
    }

which also introduces the keyword 'keyword', to 'keyword' keywords. while you keyword.
if `keyword(word)` is used for something that is *not* overridden, the expression will
return null, and otherwise the function bound to it.

'try'...'catch' minus explicit exception classifier: that is, make it behave more like javascript (boooo!)
Type introspection is already decent enough to not need explicit type information in the 'catch' clause.

Make types extensible via scripting. Example:

    String.camelCase = function(s)
    {
        return s[0].upper() + s[1 .. s.length];
    }