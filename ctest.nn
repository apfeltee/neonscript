
/*
    colors = try{ import colors }
*/

try
{
    colors = import "colors"
}
catch(e)
{
    throw Exception("FAILED TO IMPORT COLORS");
}

println("colors=", colors);

print(colors.text('Successful!', colors.text_color.green))

