


var fh = null

    var i = 1;
    fh = File("Makefile", "rb")
    //println("raw: ", raw.toString())
    while((line = fh.readLine()) != null)
    {
        if(line.matches('^\s*#\s*\b\w+\b\s*='))
        {
            println("[", i, "]: ", line);
        }
        i++;
    }
