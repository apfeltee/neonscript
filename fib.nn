function fib(n)
{
    if(n < 2)
    {
        return n
    }
    return fib(n - 2) + fib(n - 1)
}

var start = microtime()
var n = 12
println("fib(", n, "): ", fib(n),"; elapsed: ", microtime() - start)